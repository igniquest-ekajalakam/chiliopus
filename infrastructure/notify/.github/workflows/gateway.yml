name: Gateway CI

on:
  push:
    paths:
      - "services/gateway/**"
      - ".github/workflows/gateway.yml"
  pull_request:
    paths:
      - "services/gateway/**"
      - ".github/workflows/gateway.yml"

jobs:
  gateway-ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        if: ${{ hashFiles('services/gateway/package.json') != '' }}
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        if: ${{ hashFiles('services/gateway/package.json') != '' }}
        working-directory: services/gateway
        run: npm ci

      - name: Build
        if: ${{ hashFiles('services/gateway/package.json') != '' }}
        working-directory: services/gateway
        run: npm run build --if-present

      - name: No package.json found
        if: ${{ hashFiles('services/gateway/package.json') == '' }}
        run: echo "services/gateway is scaffolded; add package.json to enable full CI."

