name: Inventory CI

on:
  push:
    paths:
      - "services/inventory/**"
      - ".github/workflows/inventory.yml"
  pull_request:
    paths:
      - "services/inventory/**"
      - ".github/workflows/inventory.yml"

jobs:
  inventory-ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        if: ${{ hashFiles('services/inventory/package.json') != '' }}
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        if: ${{ hashFiles('services/inventory/package.json') != '' }}
        working-directory: services/inventory
        run: npm ci

      - name: Build
        if: ${{ hashFiles('services/inventory/package.json') != '' }}
        working-directory: services/inventory
        run: npm run build --if-present

      - name: No package.json found
        if: ${{ hashFiles('services/inventory/package.json') == '' }}
        run: echo "services/inventory is scaffolded; add package.json to enable full CI."

