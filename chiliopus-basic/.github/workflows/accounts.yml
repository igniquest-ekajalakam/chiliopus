name: Accounts CI

on:
  push:
    paths:
      - "services/accounts/**"
      - ".github/workflows/accounts.yml"
  pull_request:
    paths:
      - "services/accounts/**"
      - ".github/workflows/accounts.yml"

jobs:
  accounts-ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        if: ${{ hashFiles('services/accounts/package.json') != '' }}
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        if: ${{ hashFiles('services/accounts/package.json') != '' }}
        working-directory: services/accounts
        run: npm ci

      - name: Build
        if: ${{ hashFiles('services/accounts/package.json') != '' }}
        working-directory: services/accounts
        run: npm run build --if-present

      - name: No package.json found
        if: ${{ hashFiles('services/accounts/package.json') == '' }}
        run: echo "services/accounts is scaffolded; add package.json to enable full CI."

