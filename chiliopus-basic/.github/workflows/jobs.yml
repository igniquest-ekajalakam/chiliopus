name: Jobs CI

on:
  push:
    paths:
      - "services/jobs/**"
      - ".github/workflows/jobs.yml"
  pull_request:
    paths:
      - "services/jobs/**"
      - ".github/workflows/jobs.yml"

jobs:
  jobs-ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        if: ${{ hashFiles('services/jobs/package.json') != '' }}
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        if: ${{ hashFiles('services/jobs/package.json') != '' }}
        working-directory: services/jobs
        run: npm ci

      - name: Build
        if: ${{ hashFiles('services/jobs/package.json') != '' }}
        working-directory: services/jobs
        run: npm run build --if-present

      - name: No package.json found
        if: ${{ hashFiles('services/jobs/package.json') == '' }}
        run: echo "services/jobs is scaffolded; add package.json to enable full CI."

